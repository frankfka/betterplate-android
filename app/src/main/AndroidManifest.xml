<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.betterplate.betterplate">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AllRestaurantsActivity" />
        <activity android:name=".activity.RestaurantMenusActivity" />
        <activity android:name=".activity.MenuItemDetailsActivity" />
        <activity android:name=".activity.CurrentMealActivity" />
        <activity android:name=".RestaurantOverviewActivity"></activity>
    </application>

</manifest>